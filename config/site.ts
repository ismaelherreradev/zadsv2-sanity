export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: 'Zads',
  description: 'Solución completa de publicidad digital',
  mainNav: [
    {
      title: 'Home',
      href: '/',
    },
  ],
};
